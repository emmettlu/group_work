# FileHub Environment Configuration
# Copy this file to .env and update values

# Database Configuration
DB_HOST=mysql
DB_PORT=3306
DB_NAME=filehub
DB_USER=webapp
DB_PASS=W3bApp_P@ss2023

# SMB Configuration (for reference)
SMB_HOST=fileserver
SMB_PORT=445
SMB_SHARE=myshare
SMB_VERSION=4.6.3

# Application Settings
APP_DEBUG=false
APP_ENV=production
APP_KEY=base64:fake_key_for_demonstration

# Security Settings (TODO)
DISABLE_DEBUG_MODE=true
REMOVE_GIT_DIRECTORY=true
SECURE_BACKUP_FOLDER=true

# Note: This is a training environment
# Real credentials should never be in .env.example
