# 靶机架构设计文档

## 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         攻击者/渗透测试人员                        │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        ▼
        ┌───────────────────────────────────────┐
        │        端口扫描 (nmap)                 │
        │  发现: 80, 139, 445, 3306             │
        └───────────────┬───────────────────────┘
                        │
        ┌───────────────┴───────────────┐
        │                               │
        ▼                               ▼
┌──────────────┐              ┌──────────────────┐
│   Web 层     │              │    SMB 层        │
│  (迷惑层1)   │              │  (核心漏洞)      │
│   Port 80    │              │   Port 445       │
└──────┬───────┘              └────────┬─────────┘
       │                               │
       │                               │
       ▼                               │
┌─────────────────────┐                │
│   Nginx + PHP-FPM   │                │
│  文件管理系统界面     │                │
└──────┬──────────────┘                │
       │                               │
       ├─► 弱密码登录成功               │
       │                               │
       ▼                               │
┌─────────────────────┐                │
│   数据库层          │                │
│   (迷惑层2)         │                │
│   Port 3306         │                │
│                     │                │
│  ┌────────────────┐ │                │
│  │ system_config  │◄┼────提示────────┤
│  │ system_logs    │ │                │
│  │ system_info    │ │                │
│  │ get_server_    │ │                │
│  │   info() 存储  │ │                │
│  │   过程         │ │                │
│  └────────────────┘ │                │
└──────┬──────────────┘                │
       │                               │
       │  提示: Samba 4.6.3           │
       │  提示: CVE-2017-7494         │
       │                               │
       └───────────┬───────────────────┘
                   │
                   ▼
          ┌─────────────────┐
          │  SMB 匿名访问    │
          │  //IP/myshare   │
          └────────┬────────┘
                   │
                   ▼
          ┌─────────────────────┐
          │  server_notes.txt   │
          │  (维护文档 - 金矿)  │
          │                     │
          │  ✓ CVE详情         │
          │  ✓ 利用条件        │
          │  ✓ Exploit使用说明 │
          └────────┬────────────┘
                   │
                   ▼
          ┌─────────────────┐
          │  恍然大悟！      │
          │  CVE-2017-7494  │
          └────────┬────────┘
                   │
                   ▼
          ┌─────────────────┐
          │ 下载 exploit.py │
          │ 执行攻击        │
          └────────┬────────┘
                   │
                   ▼
          ┌─────────────────┐
          │  获得 Root Shell│
          │  靶机被攻破！    │
          └─────────────────┘
```

## 服务架构

### Docker Compose 服务

```
┌─────────────────────────────────────────────────────┐
│              Docker Compose 环境                     │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │   Web    │  │   PHP    │  │    DB    │         │
│  │  Nginx   │◄─┤  PHP-FPM │◄─┤  MySQL   │         │
│  │  :80     │  │  :9000   │  │  :3306   │         │
│  └────┬─────┘  └──────────┘  └──────────┘         │
│       │                                             │
│       │        ┌──────────────┐                    │
│       └────────┤    Samba     │                    │
│                │  CVE-2017-    │                    │
│                │    7494       │                    │
│                │  :445, :139   │                    │
│                └──────────────┘                    │
│                                                      │
│              Network: internal_net                  │
└─────────────────────────────────────────────────────┘
```

## 数据流向

### 1. 正常用户访问流程

```
用户浏览器
    ↓
http://IP:80
    ↓
Nginx (Web容器)
    ↓
FastCGI → PHP-FPM (PHP容器)
    ↓
MySQL Query → MySQL (DB容器)
    ↓
返回页面 → 用户
```

### 2. 攻击者信息收集流程

```
攻击者
    ↓
端口扫描 (nmap)
    ↓
├─► Web服务探测 (80)
│       ↓
│   弱密码登录
│       ↓
│   浏览系统页面
│       ↓
│   数据库查询
│       ↓
│   发现 Samba 线索
│
└─► SMB服务探测 (445)
        ↓
    匿名访问测试
        ↓
    下载共享文件
        ↓
    阅读维护文档
        ↓
    确认 CVE-2017-7494
        ↓
    执行 exploit
        ↓
    获得 shell
```

## 迷惑设计层次

### 第一层：Web 应用（迷惑+引导）

**目的**: 让攻击者以为是 Web 应用漏洞

**设计**:
- 登录页面 → 诱导尝试 SQL 注入（失败）
- 弱密码 → 成功登录（小胜利，继续探索）
- 文件管理系统 → 看起来是重点
- 系统公告 → 埋下 Samba 线索

**关键文件**:
- `web/html/index.php` - 登录页面
- `web/html/files/index.php` - 文件管理
- `web/html/css/style.css` - 精美界面

### 第二层：数据库（引导+提示）

**目的**: 通过数据逐步揭示 Samba 漏洞信息

**设计**:
- `system_logs` 表 → 警告信息
- `system_config` 表 → Samba 配置详情
- `system_info` 视图 → VULNERABLE 标签
- `get_server_info()` 存储过程 → 直接提示

**关键提示**:
```sql
SELECT * FROM system_config WHERE config_key = 'samba_version';
-- 返回: 4.6.3 (存在已知问题)

CALL get_server_info();
-- 返回: 建议检查CVE数据库
```

### 第三层：SMB 共享（关键线索）

**目的**: 提供完整的攻击指南

**设计**:
- 匿名访问 → 无需认证
- `server_notes.txt` → 维护文档（金矿）
- `backup_config.txt` → 配置详情
- `README.txt` → 使用说明

**关键内容**:
- CVE-2017-7494 详细说明
- 利用条件（全部满足）
- Exploit 下载地址
- 使用示例

## 漏洞利用原理

### CVE-2017-7494 技术细节

```
1. 攻击者连接到 SMB 共享
   smbclient //target/myshare -N

2. 上传恶意共享库文件 (.so)
   put payload.so

3. 利用 MS-RPC 调用触发加载
   特制的 SMB 请求 → 触发 dlopen()

4. 恶意代码执行
   payload.so 被加载并执行
   
5. 反向 Shell 连接
   攻击者获得 root 权限的 shell
```

### 利用条件验证

| 条件 | 本靶机配置 | 状态 |
|------|-----------|------|
| Samba 版本 3.5.0-4.6.4 | 4.6.3 | ✓ |
| 共享目录可写 | read only = no | ✓ |
| 匿名访问或弱认证 | guest ok = yes | ✓ |
| 445 端口可访问 | 开放 | ✓ |

## 文件映射关系

### 容器内外文件映射

```
主机文件系统                容器内路径
─────────────────────────────────────────────
./web/nginx.conf         →  /etc/nginx/nginx.conf
./web/html/              →  /var/www/html/
./db/init.sql            →  /docker-entrypoint-initdb.d/init.sql
./samba/smb.conf         →  /usr/local/samba/etc/smb.conf
./samba/share/           →  /home/share/
```

### Web 应用路由

```
URL 路径                  文件路径
─────────────────────────────────────────────
/                        →  index.php
/files/                  →  files/index.php
/admin/system_info.php   →  admin/system_info.php
/admin/logs.php          →  admin/logs.php
/admin/database.php      →  admin/database.php
/css/style.css           →  css/style.css
```

## 安全弱点矩阵

| 组件 | 弱点 | 严重程度 | 利用难度 |
|------|------|---------|---------|
| Web登录 | 弱密码 | 中 | 低 |
| Web应用 | 信息泄露 | 中 | 低 |
| 数据库 | 敏感信息暴露 | 中 | 低 |
| SMB | 匿名访问 | 高 | 低 |
| Samba | CVE-2017-7494 | 严重 | 中 |

## 攻击时间线（预期）

```
T+0min   端口扫描，发现服务
T+5min   尝试 SQL 注入（失败）
T+10min  弱密码登录成功
T+20min  浏览系统页面，发现 Samba 线索
T+30min  数据库查询，确认版本信息
T+40min  SMB 连接，下载维护文档
T+50min  阅读文档，恍然大悟
T+60min  搜索并下载 exploit
T+70min  执行 exploit
T+75min  获得 root shell
───────────────────────────────
总计: 约 1-1.5 小时
```

## 教学价值

### 技能训练

1. **信息收集** ⭐⭐⭐⭐⭐
   - 端口扫描
   - 服务枚举
   - 版本识别

2. **Web 测试** ⭐⭐⭐
   - 弱密码测试
   - 信息泄露识别

3. **数据库安全** ⭐⭐⭐⭐
   - SQL 查询技巧
   - 配置信息挖掘

4. **SMB 协议** ⭐⭐⭐⭐
   - 匿名访问测试
   - 文件枚举

5. **CVE 研究** ⭐⭐⭐⭐⭐
   - 漏洞查询
   - Exploit 使用

6. **攻击链构建** ⭐⭐⭐⭐⭐
   - 信息关联
   - 攻击路径规划

### 学习曲线

```
难度
 │
 │                           ┌─────────
 │                      ┌────┘ 漏洞利用
 │                 ┌────┘
 │            ┌────┘ CVE研究
 │       ┌────┘
 │  ┌────┘ 信息收集
 │──┘
 └─────────────────────────────────► 时间
   Web  DB  SMB  CVE  Exploit  Shell
```

## 防御建议

### 修复清单

1. **立即执行**:
   - [ ] 升级 Samba 到 4.6.5+
   - [ ] 禁用匿名 SMB 访问
   - [ ] 修改所有弱密码
   - [ ] 移除敏感文档

2. **短期内**:
   - [ ] 配置防火墙规则
   - [ ] 启用访问日志审计
   - [ ] 实施强密码策略

3. **长期规划**:
   - [ ] 建立补丁管理流程
   - [ ] 部署入侵检测系统
   - [ ] 定期安全评估

## 项目统计

- **总代码行数**: ~3000+ 行
- **配置文件**: 10+ 个
- **数据库表**: 4 个
- **Web 页面**: 6 个
- **文档**: 4 个 Markdown 文件
- **Docker 容器**: 4 个
- **开放端口**: 4 个
- **CVE 数量**: 1 个（严重）

## 总结

这个靶机通过精心设计的多层架构，成功地：

✅ 模拟了真实的企业环境  
✅ 提供了完整的攻击链路径  
✅ 包含了真实的高危 CVE 漏洞  
✅ 设置了适度的迷惑和引导  
✅ 达到了良好的教学效果  

**核心设计理念**: "让攻击者走弯路，但最终恍然大悟"

---

**设计团队**: 4人小组  
**开发周期**: 6周  
**最后更新**: 2024-01-15