[global]
    map to guest = Bad User
    server string = Samba Server
    guest account = nobody

[myshare]
    path = /home/share
    read only = no
    guest ok = yes
    guest only = yes

[private]
    path = /home/private
    read only = no
    guest ok = no
    valid users = sambauser
